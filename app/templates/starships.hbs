<div class="grid">
  <div class="left-panel">
    <ul class="list">
      {{#each @model.starships as |p|}}
        <li><LinkTo @route="starships.show" @model={{p}}>{{p.name}}</LinkTo></li>
      {{/each}}
    </ul>
  </div>
  <div class="right-panel">
    {{outlet}}
  </div>
</div>
