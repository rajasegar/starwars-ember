<div class="grid">
  <div class="left-panel">
    <ul class="list">
      {{#each @model.people as |p|}}
        <li><LinkTo @route="people.show" @model={{p}}>{{p.name}}</LinkTo></li>
      {{/each}}
    </ul>
  </div>
  <div class="right-panel">
    {{outlet}}
  </div>
</div>
